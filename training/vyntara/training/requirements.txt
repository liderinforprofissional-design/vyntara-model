# Pipeline de treino do Vyntara. Instale com:  pip install -r requirements.txt
requests>=2.31
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
# Opcional (o pipeline funciona sem, caindo para GradientBoosting do sklearn):
# xgboost>=2.0
# Camada de midia/contexto (GPT-4o-mini le as manchetes) - necessario se useNews=true:
openai>=1.30
# Fase FBref: rodar 'python fetch_fbref.py' no SEU PC (nao no GitHub Actions).
# Instale so quando for captar o FBref:  pip install soccerdata
# soccerdata>=1.8
